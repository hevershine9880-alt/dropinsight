# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/test-results
/playwright-report
/blob-report
/.playwright

# database — local only; migrations are committed, data is not
*.db
*.db-journal
prisma/dev.db*

# generated prisma client
/src/generated

# secrets
.env
.env*.local

# reference material — the source videos and the two vendored skill repos are
# kept locally for provenance, not committed. Mockups and transcripts ARE
# committed: they are the specification.
/reference/videos
/reference/skills

# os / editor
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel / typescript
.vercel
*.tsbuildinfo
scripts/.screens/
